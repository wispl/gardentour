syntax = "proto3";

option java_package = "com.example.app.datastore";
option java_multiple_files = true;

message UserPreferences {
  map<string, bool> saved_places = 1;
  map<string, bool> saved_cities = 2;
}